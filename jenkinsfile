pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
        stage('Test') {
            steps {
                sh 'npm run start:dev'
                sh 'curl -i http://localhost:3000'
            }
        }
        
    }
}
